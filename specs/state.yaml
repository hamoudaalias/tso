# specs/state.yaml — État de session TSO
# Généré par session-state (03/08/2025)

active_flow: build_epic
active_epic_id: e03
active_story_id: null
active_bug_id: null

release:
  target_version: null
  last_tag: v16.0-tabula-rasa
  last_publish: null

epic_cycle:
  current_step: develop-tdd
  next_skill: verify-work
  completed_steps:
    - kickoff-branch
    - survey-context
    - develop-tdd # e03s01, e03s02, e03s03 committed

bug_cycle:
  current_step: null
  completed_steps: []

git:
  branch: feat/e03-stationary-reward
  hash: 4d984c8
  head_commit: "fix(tso_engine): add CognitiveConfig bissection + δ-clip par défaut (5.0)"
  working_tree:
    modified: []
    untracked: []
  preflight: cargo check --bins passed (0 errors, warnings only)

handoff:
  last_step_completed: "e03s00 — CognitiveConfig + δ-clip fix, multi-seed bisection validée"
  open_decisions:
    - "e03s04 — experiment_e03.rs commité mais Configs A-D en single seed, résultats 17-32% (normal, pas un problème de δ)"
    - "Les Configs A-D utilisent replay (lr=0.05) qui peut corrompre — le fix δ-clip est validé par multi_seed_bisect (sans replay)"
    - "Prochains steps : vérifier e03s01, e03s02, e03s03 passent toujours, puis débloquer e03s04 en mode δ-clip + replay_clean"
  next_skill: develop-tdd

workspace_facts:
  - "Le projet TSO mixe lib + 18 binaires dans tso-engine/src/bin/"
  - "Les tests d'intégration sont dans tso-engine/tests/"
  - "E03 a 4 stories : diagnostic → normalisation → dual critic → expérience comparative"
  - "Master est plusieurs commits derrière feat/e03-stationary-reward"
