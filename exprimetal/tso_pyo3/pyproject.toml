[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "tso-pyo3"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
module-name = "tso_pyo3._core"
features = ["pyo3/extension-module"]
